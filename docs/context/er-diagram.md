# ER Diagram — monitoreo-v2

> Auto-generated by `scripts/generate-er-diagram.mjs` from 53 TypeORM entities.
> 106 foreign key relations detected.
>
> Re-generate: `npm run db:er-diagram`

```mermaid
erDiagram
    notification_logs }o--|| tenants : "tenant_id"
    notification_logs }o--|| alerts : "alert_id"
    api_keys }o--|| tenants : "tenant_id"
    tenant_sso_config }o--|| tenants : "tenant_id"
    building_import_jobs }o--|| tenants : "tenant_id"
    building_import_jobs }o--|| users : "created_by_user_id"
    building_import_staging_rows }o--|| building_import_jobs : "job_id"
    building_import_staging_rows }o--|| tenants : "tenant_id"
    building_import_staging_rows }o--|| regions : "resolved_region_id"
    building_import_staging_rows }o--|| buildings : "created_building_id"
    meter_import_jobs }o--|| tenants : "tenant_id"
    meter_import_jobs }o--|| users : "created_by_user_id"
    meter_import_staging_rows }o--|| meter_import_jobs : "job_id"
    meter_import_staging_rows }o--|| tenants : "tenant_id"
    meter_import_staging_rows }o--|| buildings : "resolved_building_id"
    meter_import_staging_rows }o--|| meters : "resolved_parent_meter_id"
    meter_import_staging_rows }o--|| building_hierarchy : "resolved_hierarchy_node_id"
    meter_import_staging_rows }o--|| meters : "created_meter_id"
    oauth_clients }o--|| tenants : "tenant_id"
    alert_rules }o--|| tenants : "tenant_id"
    alert_rules }o--|| buildings : "building_id"
    alert_rules }o--|| users : "created_by"
    backfill_jobs }o--|| tenants : "tenant_id"
    backfill_jobs }o--|| meters : "meter_id"
    building_hierarchy }o--|| tenants : "tenant_id"
    building_hierarchy }o--|| buildings : "building_id"
    building_hierarchy }o--|| building_hierarchy : "parent_id"
    buildings }o--|| tenants : "tenant_id"
    buildings }o--|| regions : "region_id"
    concentrator_meters }o--|| concentrators : "concentrator_id"
    concentrator_meters }o--|| meters : "meter_id"
    concentrators }o--|| tenants : "tenant_id"
    concentrators }o--|| buildings : "building_id"
    data_export_jobs }o--|| tenants : "tenant_id"
    etl_watermarks }o--|| tenants : "tenant_id"
    fault_events }o--|| tenants : "tenant_id"
    fault_events }o--|| buildings : "building_id"
    fault_events }o--|| meters : "meter_id"
    fault_events }o--|| concentrators : "concentrator_id"
    fault_events }o--|| users : "resolved_by"
    ingest_gaps }o--|| tenants : "tenant_id"
    ingest_gaps }o--|| meters : "meter_id"
    integration_sync_logs }o--|| integrations : "integration_id"
    integrations }o--|| tenants : "tenant_id"
    invoice_line_items }o--|| invoices : "invoice_id"
    invoice_line_items }o--|| meters : "meter_id"
    invoice_line_items }o--|| tenant_units : "tenant_unit_id"
    invoices }o--|| tenants : "tenant_id"
    invoices }o--|| buildings : "building_id"
    invoices }o--|| tariffs : "tariff_id"
    invoices }o--|| users : "approved_by"
    invoices }o--|| users : "created_by"
    meter_hierarchy }o--|| meters : "meter_id"
    meter_hierarchy }o--|| building_hierarchy : "hierarchy_node_id"
    meter_reading_status }o--|| meters : "meter_id"
    meter_reading_status }o--|| tenants : "tenant_id"
    meters }o--|| tenants : "tenant_id"
    meters }o--|| buildings : "building_id"
    alerts }o--|| tenants : "tenant_id"
    alerts }o--|| alert_rules : "alert_rule_id"
    alerts }o--|| buildings : "building_id"
    alerts }o--|| meters : "meter_id"
    alerts }o--|| users : "assigned_to"
    alerts }o--|| users : "acknowledged_by"
    alerts }o--|| users : "resolved_by"
    readings }o--|| tenants : "tenant_id"
    readings }o--|| meters : "meter_id"
    regions }o--|| tenants : "tenant_id"
    register_mappings }o--|| tenants : "tenant_id"
    register_mappings }o--|| protocol_types : "protocol"
    reports }o--|| tenants : "tenant_id"
    reports }o--|| buildings : "building_id"
    reports }o--|| users : "generated_by"
    scheduled_reports }o--|| tenants : "tenant_id"
    scheduled_reports }o--|| buildings : "building_id"
    scheduled_reports }o--|| users : "created_by"
    tariff_blocks }o--|| tariffs : "tariff_id"
    tariffs }o--|| tenants : "tenant_id"
    tariffs }o--|| buildings : "building_id"
    tariffs }o--|| users : "created_by"
    tenant_unit_meters }o--|| tenant_units : "tenant_unit_id"
    tenant_unit_meters }o--|| meters : "meter_id"
    tenant_units }o--|| tenants : "tenant_id"
    tenant_units }o--|| buildings : "building_id"
    tenant_units }o--|| users : "user_id"
    webhook_delivery_logs }o--|| tenants : "tenant_id"
    webhook_delivery_logs }o--|| webhook_subscriptions : "subscription_id"
    webhook_subscriptions }o--|| tenants : "tenant_id"
    role_permissions }o--|| roles : "role_id"
    role_permissions }o--|| permissions : "permission_id"
    roles }o--|| tenants : "tenant_id"
    user_building_access }o--|| users : "user_id"
    refresh_tokens }o--|| users : "user_id"
    tenant_unit_import_jobs }o--|| tenants : "tenant_id"
    tenant_unit_import_jobs }o--|| users : "created_by_user_id"
    tenant_unit_import_staging_rows }o--|| tenant_unit_import_jobs : "job_id"
    tenant_unit_import_staging_rows }o--|| tenants : "tenant_id"
    tenant_unit_import_staging_rows }o--|| buildings : "resolved_building_id"
    tenant_unit_import_staging_rows }o--|| tenant_units : "created_tenant_unit_id"
    user_import_jobs }o--|| tenants : "tenant_id"
    user_import_jobs }o--|| users : "created_by_user_id"
    user_import_staging_rows }o--|| user_import_jobs : "job_id"
    user_import_staging_rows }o--|| tenants : "tenant_id"
    user_import_staging_rows }o--|| users : "created_user_id"
    users }o--|| tenants : "tenant_id"
    users }o--|| roles : "role_id"

    notification_logs {
        uuid id PK
        varchar tenant_id
        varchar alert_id
        varchar channel
        varchar status
        text recipient "nullable"
        text subject
        text body "nullable"
        text error_message "nullable"
        timestamptz created_at
    }
    api_keys {
        uuid id PK
        varchar tenant_id
        varchar name
        varchar key_hash UK
        varchar key_prefix
        text permissions
        uuid building_ids
        int rate_limit_per_minute
        int ingress_rate_limit_per_minute "nullable"
        timestamptz expires_at "nullable"
        bool is_active
        timestamptz last_used_at "nullable"
        uuid created_by "nullable"
        timestamptz created_at
        timestamptz updated_at
    }
    tenant_sso_config {
        uuid tenant_id PK
        text issuer
        text client_id
        text metadata_url "nullable"
        text encrypted_client_secret
        text scim_webhook_secret "nullable"
        timestamptz created_at
        timestamptz updated_at
    }
    building_import_jobs {
        uuid id PK
        varchar tenant_id
        uuid created_by_user_id "nullable"
        varchar original_filename
        varchar file_format
        enum(pending_parse,ready,committing,committed,failed,cancelled) status
        int total_rows
        int valid_rows
        int error_rows
        int duplicate_rows
        int created_rows
        text error_summary "nullable"
        timestamptz committed_at "nullable"
        timestamptz created_at
        timestamptz updated_at
    }
    building_import_staging_rows {
        uuid id PK
        varchar job_id
        varchar tenant_id
        int row_number
        jsonb raw_cells
        varchar name "nullable"
        varchar code "nullable"
        text address "nullable"
        decimal area_sqm "nullable"
        varchar region_code "nullable"
        char country_code "nullable"
        varchar timezone "nullable"
        varchar external_site_id "nullable"
        varchar site_kind "nullable"
        enum(pending,valid,error,duplicate,skipped,created) status
        text error_codes
        uuid resolved_region_id "nullable"
        uuid created_building_id "nullable"
        timestamptz created_at
    }
    iot_readings {
        serial time PK
        uuid tenant_id PK
        uuid meter_id PK
        serial variable_name PK
        double precision value
        int quality
    }
    meter_import_jobs {
        uuid id PK
        varchar tenant_id
        uuid created_by_user_id "nullable"
        varchar original_filename
        varchar file_format
        enum(pending_parse,ready,committing,committed,failed,cancelled) status
        int total_rows
        int valid_rows
        int error_rows
        int duplicate_rows
        int created_rows
        text error_summary "nullable"
        timestamptz committed_at "nullable"
        timestamptz created_at
        timestamptz updated_at
    }
    meter_import_staging_rows {
        uuid id PK
        varchar job_id
        varchar tenant_id
        int row_number
        jsonb raw_cells
        varchar name "nullable"
        varchar code "nullable"
        varchar building_code "nullable"
        varchar external_site_id "nullable"
        varchar meter_type "nullable"
        varchar model "nullable"
        varchar serial_number "nullable"
        varchar phase_type "nullable"
        varchar load_category "nullable"
        varchar parent_meter_code "nullable"
        varchar hierarchy_node_name "nullable"
        smallint modbus_address "nullable"
        varchar bus_id "nullable"
        varchar uplink_route "nullable"
        varchar external_id "nullable"
        bool is_active "nullable"
        enum(pending,valid,error,duplicate,skipped,created) status
        text error_codes
        uuid resolved_building_id "nullable"
        uuid resolved_parent_meter_id "nullable"
        uuid resolved_hierarchy_node_id "nullable"
        uuid created_meter_id "nullable"
        timestamptz created_at
    }
    oauth_clients {
        uuid id PK
        varchar tenant_id
        varchar name
        varchar client_id UK
        varchar secret_hash
        varchar client_id_prefix
        text scopes
        uuid building_ids
        int token_ttl_seconds
        bool is_active
        timestamptz last_used_at "nullable"
        uuid created_by "nullable"
        timestamptz created_at
        timestamptz updated_at
    }
    alert_rules {
        uuid id PK
        varchar tenant_id
        uuid building_id "nullable"
        varchar alert_type_code
        varchar name
        text description "nullable"
        varchar severity
        bool is_active
        int check_interval_seconds
        jsonb config
        int escalation_l1_minutes
        int escalation_l2_minutes
        int escalation_l3_minutes
        bool notify_email
        bool notify_push
        bool notify_whatsapp
        bool notify_sms
        uuid created_by "nullable"
        timestamptz created_at
        timestamptz updated_at
    }
    backfill_jobs {
        uuid id PK
        varchar tenant_id
        varchar meter_id
        timestamptz from_ts
        timestamptz to_ts
        enum(pending,running,completed,failed) status
        int rows_inserted
        text error "nullable"
        timestamptz created_at
        timestamptz updated_at
    }
    building_hierarchy {
        uuid id PK
        varchar tenant_id
        varchar building_id
        uuid parent_id "nullable"
        varchar name
        varchar level_type
        int sort_order
        jsonb metadata
        timestamptz created_at
        timestamptz updated_at
    }
    buildings {
        uuid id PK
        varchar tenant_id
        varchar name
        varchar code
        text address "nullable"
        decimal area_sqm "nullable"
        bool is_active
        uuid region_id "nullable"
        char country_code "nullable"
        varchar timezone "nullable"
        varchar external_site_id "nullable"
        varchar site_kind "nullable"
        timestamptz created_at
        timestamptz updated_at
    }
    concentrator_meters {
        uuid concentrator_id PK
        uuid meter_id PK
        smallint bus_number
        smallint modbus_address "nullable"
    }
    concentrators {
        uuid id PK
        varchar tenant_id
        varchar building_id
        varchar name
        varchar model
        varchar serial_number "nullable"
        inet ip_address "nullable"
        varchar firmware_version "nullable"
        varchar status
        timestamptz last_heartbeat_at "nullable"
        bool mqtt_connected
        smallint battery_level "nullable"
        jsonb metadata
        timestamptz created_at
        timestamptz updated_at
    }
    data_contracts {
        uuid id PK
        uuid tenant_id "nullable"
        varchar name
        varchar version
        jsonb schema_json
        timestamptz effective_from
        timestamptz created_at
    }
    data_export_jobs {
        uuid id PK
        varchar tenant_id
        enum(csv,parquet) format
        enum(pending,running,completed,failed) status
        jsonb params
        varchar s3_key "nullable"
        varchar local_path "nullable"
        int row_count
        text error "nullable"
        timestamptz expires_at "nullable"
        timestamptz created_at
        timestamptz updated_at
    }
    data_quality_daily {
        uuid tenant_id PK
        uuid building_id PK
        serial day PK
        decimal measured_pct
        decimal estimated_pct
        decimal invalid_pct
        decimal unknown_pct
        bigint total
    }
    data_slo_breaches {
        uuid id PK
        varchar tenant_id
        varchar slo_type
        timestamptz breached_at
        jsonb detail
    }
    etl_watermarks {
        serial consumer_id PK
        uuid tenant_id PK
        serial stream PK
        text last_cursor
        timestamptz updated_at
    }
    fault_events {
        uuid id PK
        varchar tenant_id
        varchar building_id
        uuid meter_id "nullable"
        uuid concentrator_id "nullable"
        varchar fault_type
        varchar severity
        text description "nullable"
        timestamptz started_at
        timestamptz resolved_at "nullable"
        text resolution_notes "nullable"
        uuid resolved_by "nullable"
        timestamptz created_at
    }
    ingest_gaps {
        uuid id PK
        varchar tenant_id
        varchar meter_id
        timestamptz gap_start
        timestamptz gap_end
        timestamptz detected_at
        timestamptz resolved_at "nullable"
        enum(open,resolved) status
    }
    integration_sync_logs {
        serial id PK
        varchar integration_id
        varchar status
        int records_synced
        text error_message "nullable"
        timestamptz started_at
        timestamptz completed_at "nullable"
        timestamptz created_at
    }
    integrations {
        uuid id PK
        varchar tenant_id
        varchar name
        varchar integration_type
        varchar status
        jsonb config
        timestamptz last_sync_at "nullable"
        text error_message "nullable"
        timestamptz created_at
        timestamptz updated_at
    }
    invoice_line_items {
        uuid id PK
        varchar invoice_id
        varchar meter_id
        uuid tenant_unit_id "nullable"
        decimal kwh_consumption
        decimal kw_demand_max
        decimal kvarh_reactive
        decimal kwh_exported
        decimal net_balance
        decimal energy_charge
        decimal demand_charge
        decimal reactive_charge
        decimal fixed_charge
        decimal total_net
    }
    invoices {
        uuid id PK
        varchar tenant_id
        varchar building_id
        uuid tariff_id "nullable"
        varchar invoice_number
        date period_start
        date period_end
        varchar status
        decimal total_net
        decimal tax_rate
        decimal tax_amount
        decimal total
        text notes "nullable"
        uuid approved_by "nullable"
        timestamptz approved_at "nullable"
        varchar created_by
        timestamptz created_at
        timestamptz updated_at
    }
    meter_hierarchy {
        uuid meter_id PK
        uuid hierarchy_node_id PK
    }
    meter_reading_status {
        uuid meter_id PK
        varchar tenant_id
        timestamptz last_reading_at "nullable"
        timestamptz last_ingested_at "nullable"
        varchar last_source "nullable"
        timestamptz updated_at
    }
    meters {
        uuid id PK
        varchar tenant_id
        varchar building_id
        varchar name
        varchar code
        varchar meter_type
        bool is_active
        jsonb metadata
        varchar external_id "nullable"
        varchar model "nullable"
        varchar serial_number "nullable"
        inet ip_address "nullable"
        smallint modbus_address "nullable"
        varchar bus_id "nullable"
        varchar phase_type
        varchar di_status
        varchar do_status
        varchar uplink_route "nullable"
        int crc_errors_last_poll
        decimal nominal_voltage "nullable"
        decimal nominal_current "nullable"
        decimal contracted_demand_kw "nullable"
        varchar load_category "nullable"
        uuid parent_meter_id "nullable"
        timestamptz created_at
        timestamptz updated_at
    }
    alerts {
        uuid id PK
        varchar tenant_id
        uuid alert_rule_id "nullable"
        varchar building_id
        uuid meter_id "nullable"
        varchar alert_type_code
        varchar severity
        varchar status
        text message
        double precision triggered_value "nullable"
        double precision threshold_value "nullable"
        uuid assigned_to "nullable"
        uuid acknowledged_by "nullable"
        timestamptz acknowledged_at "nullable"
        uuid resolved_by "nullable"
        timestamptz resolved_at "nullable"
        text resolution_notes "nullable"
        timestamptz created_at
    }
    protocol_types {
        serial code PK
        varchar label
        text description "nullable"
    }
    readings {
        uuid id PK
        varchar tenant_id
        varchar meter_id
        timestamptz timestamp
        decimal voltage_l1 "nullable"
        decimal voltage_l2 "nullable"
        decimal voltage_l3 "nullable"
        decimal current_l1 "nullable"
        decimal current_l2 "nullable"
        decimal current_l3 "nullable"
        decimal power_kw
        decimal reactive_power_kvar "nullable"
        decimal power_factor "nullable"
        decimal frequency_hz "nullable"
        decimal energy_kwh_total
        decimal thd_voltage_pct "nullable"
        decimal thd_current_pct "nullable"
        decimal phase_imbalance_pct "nullable"
        varchar breaker_status "nullable"
        smallint digital_input_1 "nullable"
        smallint digital_input_2 "nullable"
        smallint digital_output_1 "nullable"
        smallint digital_output_2 "nullable"
        varchar alarm "nullable"
        int modbus_crc_errors "nullable"
        enum(measured,estimated,invalid,unknown) quality
        timestamptz ingested_at
        varchar source "nullable"
    }
    regions {
        uuid id PK
        varchar tenant_id
        varchar code
        varchar name
        char country_code
        timestamptz created_at
        timestamptz updated_at
    }
    register_mappings {
        uuid id PK
        varchar tenant_id "nullable"
        varchar protocol
        varchar device_profile
        varchar register_key
        varchar target_field
        decimal scale_factor
        varchar unit "nullable"
        timestamptz created_at
        timestamptz updated_at
    }
    reports {
        uuid id PK
        varchar tenant_id
        uuid building_id "nullable"
        varchar report_type
        date period_start
        date period_end
        varchar format
        text file_url "nullable"
        bigint file_size_bytes "nullable"
        varchar generated_by
        timestamptz created_at
    }
    scheduled_reports {
        uuid id PK
        varchar tenant_id
        uuid building_id "nullable"
        varchar report_type
        varchar format
        varchar cron_expression
        jsonb recipients
        bool is_active
        timestamptz last_run_at "nullable"
        timestamptz next_run_at "nullable"
        varchar created_by
        timestamptz created_at
        timestamptz updated_at
    }
    tariff_blocks {
        uuid id PK
        varchar tariff_id
        varchar block_name
        smallint hour_start
        smallint hour_end
        decimal energy_rate
        decimal demand_rate
        decimal reactive_rate
        decimal fixed_charge
    }
    tariffs {
        uuid id PK
        varchar tenant_id
        varchar building_id
        varchar name
        date effective_from
        date effective_to "nullable"
        bool is_active
        uuid created_by "nullable"
        timestamptz created_at
        timestamptz updated_at
    }
    tenant_unit_meters {
        uuid tenant_unit_id PK
        uuid meter_id PK
    }
    tenant_units {
        uuid id PK
        varchar tenant_id
        varchar building_id
        varchar name
        varchar unit_code
        varchar external_unit_id "nullable"
        varchar contact_name "nullable"
        varchar contact_email "nullable"
        uuid user_id "nullable"
        bool is_active
        timestamptz created_at
        timestamptz updated_at
    }
    webhook_delivery_logs {
        uuid id PK
        varchar tenant_id
        varchar subscription_id "nullable"
        varchar event_type
        text url
        varchar status
        int http_status "nullable"
        int attempt_count
        text error_message "nullable"
        jsonb payload
        timestamptz created_at
    }
    webhook_subscriptions {
        uuid id PK
        varchar tenant_id
        varchar event_type
        text url
        text secret
        bool active
        timestamptz created_at
        timestamptz updated_at
    }
    permissions {
        uuid id PK
        varchar module
        varchar action
        text description "nullable"
    }
    role_permissions {
        uuid role_id PK
        uuid permission_id PK
        varchar access_level
    }
    roles {
        uuid id PK
        varchar tenant_id
        varchar name
        varchar slug
        text description "nullable"
        int max_session_minutes
        smallint hierarchy_level
        bool is_default
        bool require_mfa
        bool is_active
        timestamptz created_at
        timestamptz updated_at
    }
    user_building_access {
        uuid user_id PK
        uuid building_id PK
    }
    refresh_tokens {
        uuid id PK
        varchar user_id
        varchar token_hash UK
        timestamptz expires_at
        timestamptz created_at
        timestamptz revoked_at "nullable"
        varchar revoked_reason "nullable"
        inet ip_address "nullable"
        text user_agent "nullable"
        timestamptz last_activity_at
    }
    tenant_unit_import_jobs {
        uuid id PK
        varchar tenant_id
        uuid created_by_user_id "nullable"
        varchar original_filename
        varchar file_format
        enum(pending_parse,ready,committing,committed,failed,cancelled) status
        int total_rows
        int valid_rows
        int error_rows
        int duplicate_rows
        int created_rows
        text error_summary "nullable"
        timestamptz committed_at "nullable"
        timestamptz created_at
        timestamptz updated_at
    }
    tenant_unit_import_staging_rows {
        uuid id PK
        varchar job_id
        varchar tenant_id
        int row_number
        jsonb raw_cells
        varchar name "nullable"
        varchar unit_code "nullable"
        varchar building_code "nullable"
        varchar external_site_id "nullable"
        varchar contact_name "nullable"
        varchar contact_email "nullable"
        varchar external_unit_id "nullable"
        enum(pending,valid,error,duplicate,skipped,created) status
        text error_codes
        uuid resolved_building_id "nullable"
        uuid created_tenant_unit_id "nullable"
        timestamptz created_at
    }
    tenants {
        uuid id PK
        varchar name
        varchar slug UK
        bool is_active
        varchar primary_color
        varchar secondary_color
        text logo_url "nullable"
        text favicon_url "nullable"
        varchar app_title
        varchar sidebar_color
        varchar accent_color
        varchar address "nullable"
        varchar address_detail "nullable"
        varchar phone "nullable"
        varchar tax_id "nullable"
        jsonb settings
        varchar timezone
        char default_country_code "nullable"
        char default_currency "nullable"
        timestamptz created_at
        timestamptz updated_at
    }
    user_import_jobs {
        uuid id PK
        varchar tenant_id
        uuid created_by_user_id "nullable"
        varchar original_filename
        varchar file_format
        enum(pending_parse,ready,committing,committed,failed,cancelled) status
        int total_rows
        int valid_rows
        int error_rows
        int duplicate_rows
        int created_rows
        bool age_verified_at_commit
        text error_summary "nullable"
        timestamptz committed_at "nullable"
        timestamptz created_at
        timestamptz updated_at
    }
    user_import_staging_rows {
        uuid id PK
        varchar job_id
        varchar tenant_id
        int row_number
        jsonb raw_cells
        varchar email "nullable"
        varchar display_name "nullable"
        varchar auth_provider "nullable"
        varchar role_slug "nullable"
        text building_codes_raw "nullable"
        varchar phone "nullable"
        enum(pending,valid,error,duplicate,skipped,created) status
        text error_codes
        uuid resolved_role_id "nullable"
        uuid resolved_building_ids
        uuid created_user_id "nullable"
        timestamptz created_at
    }
    users {
        uuid id PK
        varchar tenant_id
        varchar email
        varchar display_name "nullable"
        varchar auth_provider
        varchar auth_provider_id "nullable"
        varchar role_id
        bool is_active
        varchar mfa_secret "nullable"
        bool mfa_enabled
        text mfa_recovery_codes "nullable"
        timestamptz privacy_accepted_at "nullable"
        varchar privacy_policy_version "nullable"
        bool data_processing_blocked
        text block_reason "nullable"
        timestamptz blocked_at "nullable"
        bool opt_out_automated_decisions
        bool age_verified
        timestamptz last_login_at "nullable"
        timestamptz created_at
        timestamptz updated_at
    }
```
