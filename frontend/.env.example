# Sin esta variable: el front usa baseURL `/api` y Vite proxy (vite.config.ts) → API en AWS.
# Con backend local en marcha (npm run start:dev en backend, puerto 4000):
# VITE_API_BASE_URL=http://localhost:4000/api
