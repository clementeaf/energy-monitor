{
  "name": "iot-ingest",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "tsc",
    "deploy": "npm run build && npx serverless deploy --verbose",
    "invoke": "npx serverless invoke -f ingest --log"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.700.0",
    "pg": "^8.13.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.145",
    "@types/node": "^20.17.0",
    "@types/pg": "^8.11.0",
    "serverless": "^3.40.0",
    "typescript": "^5.6.0"
  }
}
